@model MvcProject.Models.Contact

@{
    ViewData["Title"] = "Datails";
}

<h2>Contact details</h2>

<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(x => x.Name)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(x => x.Name)
        </dt>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(x => x.Number)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(x => x.Number)
        </dt>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(x => x.IsActive)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(x => x.IsActive)
        </dt>
    </dl>
</div>

<a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
<a asp-action="Index">Back to Contact List</a>